services:
  mcp-server:
    build: .
    container_name: anthuan-mcp-server
    # Keep container running and interactive for Stdio communication
    stdin_open: true # -i
    tty: true        # -t
    restart: unless-stopped
    volumes:
      - ./data:/app/data
